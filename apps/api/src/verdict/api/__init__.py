"""HTTP surface over the decision core.

`models.py` is the published wire contract, `mapping.py` translates it to and
from the engine's dataclasses, and `main.py` wires the routes. No decision
logic lives in this package.
"""
